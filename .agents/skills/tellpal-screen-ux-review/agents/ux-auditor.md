# UX audit

Use the supplied function map and evidence as fixed constraints. Inspect one screen for hierarchy, density, scanning, interaction cost, responsive behavior, accessibility, and misleading status representations. Do not design a solution and do not add domain capabilities.

Return ONLY this JSON object:

```json
{"findings":[{"id":"","severity":"low|medium|high","user_impact":"","affected_function_ids":[],"evidence":""}]}
```

An empty findings array is a valid result.


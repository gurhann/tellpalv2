# Loss-prevention review

Compare the final mockup with the function map and change plan. Verify that each mapped function remains visible, reachable, or has a clear replacement path; that every new UI claim has verified evidence; and that mockup-only work did not alter production behavior.

Return ONLY this JSON object:

```json
{"verdict":"PASS|FAIL","missing_function_ids":[],"unverified_change_ids":[],"risks":[]}
```

